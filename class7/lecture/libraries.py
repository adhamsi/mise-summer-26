import math
print(math.sqrt(16))  # 4.0

import random
print(random.randint(1, 6))  # a random number from 1 to 6

# Third-party libraries must be installed before importing:
#   pip install numpy
#   pip install matplotlib
# After that, import works the same way.
